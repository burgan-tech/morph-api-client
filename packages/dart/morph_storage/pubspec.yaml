name: morph_storage
description: Storage adapters for Morph (Dart parity with @morph/browser-storage).
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  morph_core:
    path: ../morph_core

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
